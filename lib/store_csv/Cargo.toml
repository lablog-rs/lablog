[package]
name = "lablog_store_csv"
version = "0.1.0"
authors = ["Alexander Thaller <alexander.thaller@trivago.com>"]

[dependencies]
csv = "1.0.0-beta.3"
lablog_store = { path = "../store" }
log = "0.3"

[dev-dependencies]
tempdir = "0.3"
chrono = { version = "0.4", features = ["serde"] }
